const theme = {
  id: 'lightTheme', // Added this so we can identify the theme by name.
  primaryColor: '#2c3e50',
  secondaryColor: '#2a5298',
  bodyBackgroundColor: 'white',
  bodyFontColor: 'black',
};

export default theme;
